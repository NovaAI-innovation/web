NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_PHONE=+17809348696
NEXT_PUBLIC_EMAIL=info@chimeraenterprise.ca
OWNER_EMAIL=info@chimeraenterprise.ca
LEADS_DATA_FILE_NAME=leads.json

# PostgreSQL (required)
DATABASE_URL=postgresql://chimera:password@localhost:5432/chimera_production
POSTGRES_PASSWORD=changeme

# Agent Mail — replaces Resend
AGENT_MAIL_API_KEY=
AGENT_MAIL_FROM_EMAIL=noreply@chimeraenterprise.ca
AGENT_MAIL_FROM_NAME=Chimera Enterprise

# Session security
SESSION_SECRET=change-me-to-32-random-bytes-hex

ONCALL_ALERT_WEBHOOK_URL=
OTEL_HTTP_ENDPOINT=

# xAI (Grok) — Portal agent
XAI_API_KEY=

# Admin portal — used only for initial seed (migrate to DB user via npm run db:seed)
ADMIN_PASSWORD=
ADMIN_EMAIL=admin@chimeraenterprise.ca
